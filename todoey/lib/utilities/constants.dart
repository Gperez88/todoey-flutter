import 'package:flutter/material.dart';

const kTopBorderRadiusDecoration = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.only(
      topRight: Radius.circular(16.0),
      topLeft: Radius.circular(16.0)
  ),
);